This ZIP contains the codebase and selected saved processed artefacts required to run the /artifact-demo dissertation artefact. It excludes the full 39GB processed data folder, raw H&M CSV files, dependency folders, caches, logs, and local environment files. The /artifact-demo route is pinned to saved seed99_robustness and seed99_full_retry artefacts and does not rerun experiments or generate live recommendations.